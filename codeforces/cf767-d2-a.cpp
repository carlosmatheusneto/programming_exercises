#include <iostream>
#include <vector>
#include <algorithm>

int main(){
  int n = 0, next = 0;
  std::cin >> n;
  next = n;
  std::vector<int> v;
  for(int i = 0; i < n; i++){
    int x;
    std::cin >> x;
    v.push_back(x);
    std::sort(v.begin(), v.end()); 
    if(x == next){
      for(int i = 0; i < length)   
    }
  }
  return 0; 
}
